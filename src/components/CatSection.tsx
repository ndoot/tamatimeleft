import styled from "@emotion/styled";
import React from "react";

interface Props {}

const CatSection = (props: Props) => {
  const {} = props;
  const StyledCatSection = styled.div`
  
  
  `
  return (
    <StyledCatSection></StyledCatSection>
  );
};

export default CatSection;
